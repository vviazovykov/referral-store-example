package com.example.refferalstore.model;

public enum Currency {

    // TODO: we can add more currencies and load them from a text file or store them in the database

    USD,
    EUR,
    JPY,
    GBP,
    CHF
}